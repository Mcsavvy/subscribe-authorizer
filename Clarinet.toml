[project]
name = "subscribe-authorizer"
authors = ["Web3 Ecosystem Team"]
description = "A decentralized authorization and subscription management platform"
telemetry = false
requirements = []
[contracts.subscription-manager]
path = "contracts/subscription-manager.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
